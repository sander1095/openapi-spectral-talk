# Development: Use the local distributed ruleset
# This allows testing the ruleset before publishing to GitHub Packages
# After publishing, update this to: extends: ["@sander1095/openapi-spectral-ruleset"]
extends: ["./distributed-ruleset/.spectral.yml"]
